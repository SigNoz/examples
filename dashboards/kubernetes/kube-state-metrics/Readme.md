# Kube-State-Metrics Dashboard

The Kube-State-Metrics dashboard consists of one chart that would help to provide insights into cluster orchestration health.

- Use `k8s-state-metrics.json` for a Kube-State-Metrics dashboard.
