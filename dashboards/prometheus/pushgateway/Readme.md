# Prometheus Pushgateway Dashboard

This dashboard consist of end-to-end visibility into backup and ETL job health—tracking duration, size, throughput, failures, and cleanup activity via Prometheus Pushgateway metrics.

- Use `prometheus-pushgateway.json` for a Prometheus Pushgateway dashboard.
